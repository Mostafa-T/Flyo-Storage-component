

const progressBar = document.querySelector('.progress__bar');

window.addEventListener('load', () => {

  // Fill the progress bar when the document is loaded
  progressBar.classList.add('active');
});